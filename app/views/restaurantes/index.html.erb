<%= link_to 'Novo', action: 'new' %>

<br/>

<h1>Listagem de Restaurantes</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Nome</th>
    <th>Endereço</th>
    <th>Especialidade</th>
  </tr>

  <% @restaurantes.each do |restaurante| %>
    <tr>
      <td><%= link_to restaurante.id, action: 'show',
              id: restaurante %></td>
      <td><%= restaurante.nome %></td>
      <td><%= restaurante.endereco %></td>
      <td><%= restaurante.especialidade %></td>
      <td><%= link_to 'Deletar',
              {action: 'destroy', id: restaurante},{method: "delete"}
           %>
      </td>
      <td>
          <%= link_to 'Editar', action: 'edit', id: restaurante %>
      </td>
    </tr>
  <% end %>
</table>
